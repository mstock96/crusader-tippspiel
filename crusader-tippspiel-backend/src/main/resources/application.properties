spring.application.name=crusader-tippspiel-backend
spring.profiles.active=dev

# --- DATENBANK (Lokal H2, Live dann PostgreSQL via Docker) ---
spring.datasource.url=${DB_URL:jdbc:h2:file:./tippspiel_db}
spring.jpa.hibernate.ddl-auto=${DB_DDL:update}

# --- SERVER & SICHERHEIT ---
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
jwt.secret=${JWT_SECRET:MeinSuperGeheimerSchluesselDerMindestens32ZeichenLangSeinMuss12345}